# mdkanban — Markdownカンバンビューワー

## 目的・概要
Markdownファイルを読み込むだけで、見出しと箇条書きをそのままカンバンボードとして可視化するブラウザ完結型ビューワー。Obsidian Kanbanプラグイン互換のMarkdownを「ログイン不要・全部ローカル・サーバー送信なし」で開けることを最大の価値とする。タスク管理SaaS（Trello/Notion等）の代替を目指すのではなく、「手元のMarkdownをカンバン形式で眺めたい」という限定ニーズに特化する。

## 対象ユーザーとユースケース

### 想定ユーザー（ペルソナ）
- **P1: Obsidianユーザー** — Obsidian Kanbanプラグインで作ったボードを、Obsidianを開けない環境（共有PC・スマホブラウザ）で閲覧したい
- **P2: テキスト派エンジニア／ライター** — タスクをMarkdownでGit管理しており、レビューや進捗確認時にビジュアルなカンバン表示で俯瞰したい
- **P3: 情報セキュリティに敏感なユーザー** — タスク内容を外部SaaSにアップロードしたくないが、生のMarkdownを読むのは見づらいと感じている

### 主なユースケース
1. 手元の `kanban.md`（Obsidian Kanban形式）をドラッグ&ドロップして、出先のブラウザで内容を確認する
2. GitHub上のIssue一覧をMarkdown化したファイルを開き、列ごとの分布を一目で把握する
3. プロジェクトのREADMEに含めた進捗マークダウンを、関係者にURLとファイル両方で共有しブラウザで読んでもらう

## 既存ツールとの関係
- **重複・類似ツール有無**: 無し（既存8ツールはパスワード／絵文字／Diff／配色／QR／印鑑／あみだくじ／写真フィルターで、ドキュメント系は未提供）
- **mdeditorとの関係**: 完全に独立。mdeditorはまだ未リリース／リリースされない可能性があるため、依存・相互参照は行わない
- **クロスリンク候補**:
  - `diff/`（テキスト比較）— 「カンバンmdの2バージョンを比較したいときはDiffへ」という導線をFAQ／本文に設置
  - `colorpallet/`（カラーパレット）— カードのタグ色設定の参考として軽く触れる程度（FAQで1箇所）

## Markdown→Kanban変換ルール（核心仕様）

### 基本構造（Obsidian Kanban Plugin互換）
```
---
kanban-plugin: basic
---

# ボードタイトル（任意・H1）

## 列名1（H2）

- [ ] 未完了タスク
- [x] 完了タスク

## 列名2（H2）

- 箇条書きのみのカード（チェックボックスなし）
```

### 変換ルール詳細

| Markdown要素 | Kanban上の扱い |
|---|---|
| YAML frontmatter | パースして無視。`kanban-plugin: basic` の有無は問わない。frontmatter内の `title` キーがあればボードタイトルに採用（任意） |
| `# 見出し`（H1） | ボードタイトル（ヘッダーに表示）。複数あっても最初の1つのみ採用 |
| `## 見出し`（H2） | **列**。出現順に左から並べる |
| `### 以下`（H3〜H6） | カード本文の見出しとしてレンダリング（列やカードの区切りにはしない） |
| `- [ ] テキスト` | 未完了カード（タイトル=テキスト） |
| `- [x] テキスト` または `- [X] テキスト` | 完了カード。**取り消し線＋薄色（不透明度50%程度）で表示**。列は変えない |
| `- テキスト` | チェックボックスなしカード（未完了扱い） |
| 箇条書きのネスト（カードの子要素） | サブタスクとして扱う。表示モードに応じて：<br>・コンパクト表示：「完了/総数」のバッジのみ（例：`2/5`）<br>・詳細表示：チェックボックス付きで列挙（読み込み専用、操作不可） |
| カード直下の段落・コードブロック・リンク・画像 | カード詳細ビュー（カードクリックで開くモーダル）でMarkdownレンダリングして表示 |
| カード本文中の `#tag` | カード下部にバッジ表示。クリックで全カードに対するタグフィルタを発動 |
| カード本文中の `@YYYY-MM-DD` | 期限バッジ。今日基準で：<br>・**期限切れ（過去）：赤**<br>・**当日：黄**<br>・**未来：灰**<br>形式: `@2026-05-01` |
| カード本文中の `@文字列`（日付以外） | MVPでは**通常テキストとして扱う**（担当者機能は次フェーズ） |

### 寛容パース方針
- frontmatterが無いMarkdownでも動作する
- H2が1つも無い場合は「全カードを `カード` という単一列に入れる」フォールバックを行う
- H2より前にある箇条書きは「列に属さない」ため、`未分類` 列にまとめる
- インデントはスペース2/4、タブのいずれも許容

### サンプルmd → 変換結果のペア例

**入力 (`example.md`)**
```markdown
---
kanban-plugin: basic
---

# 春のリリース計画

## TODO

- [ ] ロゴ刷新 #design @2026-05-10
  - [ ] 案出し
  - [ ] レビュー会
- [ ] OGP画像生成 #seo

## Doing

- [ ] ドキュメント整備 @2026-04-25
  詳細はこちらを参照: [社内Wiki](https://example.com)

## Done

- [x] 仕様書作成 #planning
- [x] 関係者ヒアリング
```

**変換結果（UI上の表現）**
- ヘッダー: 「春のリリース計画」
- 列: TODO（2件）／Doing（1件）／Done（2件・取り消し線表示）
- TODOの1枚目「ロゴ刷新」: タグ `#design`、期限 `2026-05-10`（灰）、サブタスク `0/2` バッジ
- Doingの「ドキュメント整備」: 期限 `2026-04-25`（赤・期限切れ）、カードクリックでWikiリンク付き本文がモーダル表示
- Doneの2枚: 取り消し線＋50%不透明度

## 機能要件

### 必須機能（MVP）

#### F1. ファイル読み込み
- F1-1: `<input type="file" accept=".md,.markdown,text/markdown">` でファイル選択
- F1-2: ページ全体（または明示的な大きなドロップゾーン）への**ドラッグ&ドロップ**で `.md` を読み込む
- F1-3: ドラッグオーバー中はドロップゾーンがハイライトされ、「ここに離すと読み込み」というメッセージを表示
- F1-4: Chromiumブラウザで `window.showOpenFilePicker` が利用可能な場合は「ファイルを開く（推奨）」ボタンも併設（オプション）
- F1-5: 読み込んだファイル名をヘッダーに表示

#### F2. パース＆カンバン表示
- F2-1: 上記「Markdown→Kanban変換ルール」に基づき、列とカードを描画
- F2-2: 列は横並び、列内は縦に積まれたカード
- F2-3: カードはクリックで詳細モーダル（フルMarkdownレンダリング）を開ける
- F2-4: タグバッジクリックで該当タグを持つカードのみ表示するフィルタ
- F2-5: 期限バッジは色分けして視認性を高める

#### F3. 表示設定
- F3-1: テーマ切替（**ライト／ダーク**）。トグルボタン1つで切替
- F3-2: カード表示密度切替（**コンパクト／詳細**）。コンパクトはタイトル＋バッジのみ、詳細は本文冒頭1〜2行のプレビュー含む
- F3-3: タグフィルタ解除ボタン

#### F4. 永続化（LocalStorage）
- F4-1: 最後に開いたMarkdownの**内容**を保存し、次回訪問時に「前回開いたファイルを復元しますか？」と提示。承諾で復元、ファイル名も併せて保存
- F4-2: テーマ・カード密度設定を保存
- F4-3: タグフィルタ状態・カードモーダルの開閉状態は保存しない（セッション内のみ）

#### F5. オンボーディング／空状態
- F5-1: 初回訪問・ファイル未読込時はドロップゾーン中央に「Markdownファイルをドラッグ＆ドロップ、またはここをクリックして選択」と表示
- F5-2: 「サンプルを表示」ボタンを設置し、上記サンプルmdをロードしたデモ表示が可能
- F5-3: 初回オンボーディングとして「対応Markdown形式」の簡易ガイド（`## が列、- がカード` の最小例）を空状態画面に常時表示

#### F6. レスポンシブ＆タッチ対応
- F6-1: PC幅では列を横並び、画面幅を超えたら横スクロール
- F6-2: モバイル幅（〜599px）でも横並びを維持し、横スクロールで列を移動できる
- F6-3: タッチ操作で列の横スクロールが慣性スクロール（`-webkit-overflow-scrolling: touch` 等の標準的な配慮）でスムーズに動作する
- F6-4: カードのモーダルはモバイル幅では画面ほぼ全面を占めるダイアログとして表示

### オプション機能（次フェーズ候補）
- O1: カードのドラッグ&ドロップ並べ替え＋Markdownへの**書き戻し**（File System Access APIでの上書き保存）
- O2: 担当者記法 `@username` の対応（`@日付`との曖昧性回避ルールを設計の上で）
- O3: 複数ファイルの切替（タブ式）
- O4: GitHub URL貼り付けによる読み込み（Raw URLサポート）
- O5: カンバンPNGエクスポート
- O6: カードのインライン編集（パース結果を再シリアライズ）

## 非機能要件
- ブラウザ完結 / 日本語UI / 商用利用可ライセンスのみ
- レスポンシブ対応（モバイル375px〜）
- アクセシビリティ：キーボード操作（Tab／Enter／Escでモーダル閉じる）、aria属性、4.5:1以上のコントラスト
- SEO（OGP共通画像、FAQ／HowTo／WebApplication／BreadcrumbListのJSON-LD）
- パフォーマンス：1000カード規模のmdをパース＆描画して操作がもたつかないこと
- セキュリティ：読み込んだMarkdown内のHTML／script／`javascript:` URLは無害化（XSS防止）。画像のsrcは `http(s):` と `data:` のみ許可

## 入出力の定義

### 入力
- **形式**: `.md` / `.markdown` テキストファイル（UTF-8想定）
- **上限**: 1ファイル5MBまで（超過時はエラー表示）
- **バリデーション**:
  - 拡張子チェック（`.md` / `.markdown` 以外は警告のうえ続行可）
  - 文字コード判定不可時はUTF-8で読み込みを試行、失敗時はエラー
  - 中身が空文字列／H2が無くて箇条書きも無い場合は「カンバン化できる要素がありません」と表示

### 出力
- **画面表示のみ**（MVPではダウンロード／コピー／共有なし）
- LocalStorageへ最後の内容と表示設定のみ保存

## UI・UX要件

### 画面構成
1. **ヘッダー**: ツール名「mdkanban」／読み込み済みファイル名／テーマトグル／密度トグル／「ファイルを開く」ボタン／「サンプル」ボタン
2. **メインエリア**:
   - ファイル未読込時 → 大きなドロップゾーン＋オンボーディング案内
   - 読み込み後 → カンバンボード（列の横並び）
3. **タグフィルタバー**（タグが存在する場合のみ表示）: アクティブなタグと「クリア」ボタン
4. **フッター**: 既存ツール群と同じスタイルのリンク群＋著作権表示

### 操作フロー
1. ユーザーがページを開く → 空状態（前回ファイルがあれば復元提案バナー）
2. ファイルをドロップ／選択／サンプルクリック → 即座にパースしてカンバン描画
3. カードをクリック → 詳細モーダルで全Markdownを整形表示
4. タグバッジをクリック → そのタグのカードのみに絞り込み、上部にフィルタ状態表示
5. テーマ／密度トグル → 即座に反映、設定はLocalStorageに保存
6. 別のファイルを開く → 既存表示を破棄して新ファイルを描画

### フィードバック
- **成功**: パース完了時、列数とカード数を `3列・12枚を表示中` のように小さくトースト or ヘッダーに表示
- **エラー**: ファイル形式不正・サイズ超過・パース失敗時、画面中央にエラーパネル＋復旧ボタン（再選択）
- **警告**: H2が無くて単一列フォールバックを使った場合、ボード上部に `## 見出しが見つからなかったため、すべてのカードを1列にまとめました` と非破壊的に通知

### エッジケース
- 空ファイル: 「ファイルが空です」エラー
- frontmatterのみ: 「カンバン化できる要素が見つかりません」エラー
- H2はあるが箇条書きが0件: 空の列を表示（エラーにしない）
- 巨大ファイル（5MB超）: 「ファイルサイズが上限を超えています」エラー
- バイナリファイル: 「テキストファイルとして読み込めませんでした」エラー
- カード本文が極端に長い: コンパクト表示でも崩れないよう `text-overflow: ellipsis` でクリップ。詳細はモーダルで全文表示
- ネストが深いサブタスク: 表示モーダルでは2階層まで描画（それ以上はフラット化を検討）

## 画面遷移・状態管理

### 状態
- `unloaded`（未読込・初回）
- `restorable`（LocalStorageに前回ファイルあり、復元提案バナー表示中）
- `loading`（パース中、サイズ大の場合の一瞬）
- `loaded`（カンバン表示中）
- `error`（読み込み／パースエラー）

### 遷移
- `unloaded` → ファイル選択 → `loading` → 成功で `loaded`、失敗で `error`
- `restorable` → 「復元する」で `loaded`、「閉じる」で `unloaded`
- `error` → ファイル再選択で `loading` へ
- `loaded` → 別ファイル選択で `loading` へ

## 既存ツール群との連携

- **mdeditor非依存**: 仕様・コード・リンクのいずれも依存しない
- **diffへの送客**: ボード下部のFAQで「2つのカンバンmdを比較したい時は？」→「テキスト比較ツール」へリンク
- **colorpalletへの言及**: FAQで「タグの配色を考える時は？」→「カラーパレット生成」へリンク（軽め）
- **フッターナビ**: 既存全ツールのフッターに `mdkanban` を追加。本ツールも他ツール同様のフッター構成にする
- **ルートページ**: `index.html` のtools-gridに `mdkanban` カードを追加（アイコン候補：`📋` / `🗂️` / `🪧`、最終決定はdeveloper）

## SEO/メタデータ要件（CLAUDE.mdチェックリスト準拠）

### title / description案
- **title**: `mdkanban - Markdownをそのままカンバン表示｜ログイン不要・ブラウザ完結ビューワー`
- **description**: `Obsidian Kanban互換のMarkdownファイルをドラッグ&ドロップするだけでカンバンボード表示。ログイン不要・インストール不要・サーバー送信なし。スマホでも閲覧できる無料Webツール。`

### ターゲットキーワード（本文・見出しに自然に含める）
- 主軸: `markdown カンバン`、`md カンバン化`、`カンバン ブラウザ ログイン不要`
- 副軸: `Obsidian Kanban ブラウザ`、`Markdown カンバン ビューワー`、`md ファイル カンバン 表示`
- 避けるキーワード: `タスク管理`、`kanban 作成`、`Trello 代替`（激戦＆訴求軸とずれるため使わない）

### OGP / Twitter Card
- 共通OG画像: `https://kiricab.github.io/common/og-image.svg` を参照
- og:type=`website`、og:locale=`ja_JP`、og:site_name=`KIRICAB Tools`
- twitter:card=`summary_large_image`

### Canonical
- `https://kiricab.github.io/mdkanban/`

### preconnect
- `https://www.googletagmanager.com`
- `https://pagead2.googlesyndication.com`
- 採用するMarkdownパーサーCDNドメイン（jsDelivr / unpkg等、developer選定後に追加）

### 構造化データ（JSON-LD）
1. **WebApplication**
   - `applicationCategory`: `UtilitiesApplication`（または `BusinessApplication`、developer判断）
   - `operatingSystem`: `Any`
   - `image`: `https://kiricab.github.io/common/og-image.svg`
   - `offers.price`: `0` / `priceCurrency`: `JPY`
2. **BreadcrumbList**: ホーム → mdkanban の2段
3. **FAQPage**: 後述のFAQ項目案と1対1対応
4. **HowTo**: 後述の使い方ステップと1対1対応

### `seo-content` セクション内容

#### 使い方（HowToと対応・3〜5ステップ）
1. mdkanbanのページを開く
2. Markdownファイル（`.md`）をドラッグ&ドロップする、または「ファイルを開く」ボタンから選択する
3. 自動でカンバンボードとして表示される（`## 見出し`が列、`-` 箇条書きがカード）
4. カードをクリックすると詳細を表示。タグや期限が含まれていれば自動でバッジ化される
5. テーマ・表示密度はヘッダーから切替できる（設定はブラウザに保存）

#### よくある質問（FAQ項目案）
1. **どんなMarkdown形式に対応していますか？**
   → Obsidian Kanbanプラグインの `## 見出し`＝列、`- [ ] タスク`＝カード形式に対応。frontmatterもパースして無視します。
2. **アップロードしたファイルはサーバーに送信されますか？**
   → いいえ。すべての処理はお使いのブラウザ内で完結します。ファイルは外部に送信されません。
3. **編集や保存はできますか？**
   → 現バージョンは閲覧専用です。編集機能は今後のアップデートで検討中です。
4. **2つのカンバンmdの差分を比較したいときは？**
   → テキスト比較ツール「[diff](../diff/index.html)」をご利用ください。同じkiricab.github.ioで提供しています。
5. **タグや期限の色はカスタマイズできますか？**
   → 期限は今日基準で自動色分け（赤＝期限切れ・黄＝当日・灰＝未来）です。タグの配色を考える時は「[カラーパレット生成](../colorpallet/index.html)」もご活用ください。
6. **スマートフォンでも使えますか？**
   → はい。横スクロールで列を移動でき、カード詳細はモバイル最適化されたモーダルで表示します。
7. **対応していない記法はありますか？**
   → 担当者記法 `@username` や複数H1ボードなどは現バージョン非対応です。期限記法 `@YYYY-MM-DD` のみ特別扱いします。

### 関連ツールへの内部リンク（最低2箇所、本文または FAQ 内に自然に配置）
- 本文「使い方」セクション末尾またはFAQ4で `diff/`
- FAQ5で `colorpallet/`

## 採用候補ライブラリ（方向性提示）
最終判断はdeveloperに委ねる。すべてMITまたは同等のpermissiveライセンスであることが必須。

| 用途 | 候補 | ライセンス | コメント |
|---|---|---|---|
| Markdownパーサー | marked | MIT | 軽量・CDN提供あり・拡張容易 |
|  | markdown-it | MIT | プラグインエコシステムが豊富 |
|  | micromark | MIT | CommonMark厳密・モダン |
| サニタイザ | DOMPurify | Apache 2.0 / MPL 2.0 | XSS対策に必須レベル |
| ドラッグ&ドロップ | HTML5 Drag and Drop API（標準） | — | MVPはこれで十分。並べ替えは次フェーズ |
| ファイルピッカー | File System Access API（標準） | — | Chromium系のみオプション利用 |

## 受け入れ基準
- [ ] AC1: サンプルボタンをクリックするとサンプルMarkdownがロードされ、列とカードが描画される
- [ ] AC2: `## TODO` `## Doing` `## Done` を含むmdをドロップすると、3列のカンバンが左から順に表示される
- [ ] AC3: `- [x] 完了タスク` を含むカードは取り消し線＋不透明度50%程度で表示される
- [ ] AC4: `#tag` を含むカードはタグバッジが下部に表示され、バッジクリックで該当タグのカードのみに絞り込まれる
- [ ] AC5: `@2026-01-01`（過去日付）を含むカードは赤色の期限バッジが表示される
- [ ] AC6: ネストした箇条書き（サブタスク）を含むカードはコンパクト表示時に `0/2` 形式の進捗バッジが表示される
- [ ] AC7: カードをクリックすると詳細モーダルが開き、本文中のリンク・コードブロックが整形表示される
- [ ] AC8: 5MBを超えるファイルをドロップすると「ファイルサイズが上限を超えています」エラーが表示され、画面が破壊されない
- [ ] AC9: 空ファイルをドロップすると「ファイルが空です」エラーが表示される
- [ ] AC10: H2が無いMarkdownを読み込むと、警告バナー付きで全カードが単一列に表示される
- [ ] AC11: テーマトグルをクリックするとライト／ダークが切り替わり、リロード後も維持される
- [ ] AC12: 一度ファイルを開いた後にリロードすると、復元提案バナーが表示され、承諾でカンバンが復元される
- [ ] AC13: モバイル幅（375px）で列が横スクロールでき、レイアウトが崩れない
- [ ] AC14: キーボードのみで「ファイル選択ボタンへフォーカス→Enter→ファイル選択」と「カードへフォーカス→Enter→モーダル開く→Escで閉じる」が完結する
- [ ] AC15: 読み込んだMarkdownに `<script>alert('xss')</script>` が含まれていてもスクリプトは実行されず、テキストとしてエスケープ表示される
- [ ] AC16: コンソールエラーが発生しない（パース失敗時の正常系エラー表示は除く）
- [ ] AC17: ヘッダー・フッターのリンクから他ツール（diff・colorpallet等）に遷移できる
- [ ] AC18: ページのソースに WebApplication / BreadcrumbList / FAQPage / HowTo の4種類のJSON-LDが存在する

## スコープ外（今回は実装しない）
- カードのドラッグ&ドロップ並べ替え
- Markdownへの書き戻し／編集／インライン編集
- 担当者記法 `@username` の専用UI
- 複数ファイル同時オープン／タブ切替
- GitHub Raw URL読み込み
- カンバンPNG／PDFエクスポート
- リアルタイム共同編集
- ボードテンプレートの提供
- mdeditorとの連携機能（mdeditor未リリースのため）

---

## 開発者向け引き継ぎサマリ

- **新規ディレクトリ名**: `mdkanban/`
- **必要な共通アセット参照**:
  - `<link rel="stylesheet" href="../common/style.css">`（ツール固有CSSより前にリンク）
  - `<link rel="icon" href="../common/favicon.svg">`（インラインSVGや独自ファビコン禁止）
  - `<meta property="og:image" content="https://kiricab.github.io/common/og-image.svg">`
- **既存ツールからの流用が妥当な要素**:
  - 全ツール共通の `--primary-color: #1e3a8a` 等のCSSカスタムプロパティ（`common/style.css`）
  - `colorpallet/` のテーマ切替実装パターン（ライト／ダークがあれば参考に）
  - `qrgen/` または `filmfilter/` のファイル入力UI（input + ドロップゾーン併用）パターン
  - フッターの `footer-links` レイアウトはルート `index.html` と全ツール共通
- **ライブラリ選定の最終判断**: developerが行う。MIT/Apache2.0/BSD/ISC/CC0/OFL等のpermissiveライセンスのみ。CDN（jsDelivr / unpkg / cdnjs等）経由で読み込み、`package.json` は作らない
- **CLAUDE.mdチェックリストの遵守必須事項**:
  1. `mdkanban/index.html` `script.js` `style.css` の3ファイル構成を踏襲
  2. ルート `index.html` の `tools-grid` に `mdkanban` カードを追加（アイコン・コピーは要相談）
  3. 既存全ツールの `index.html` フッター `footer-links` に `mdkanban` リンクを追加
  4. `sitemap.xml` に `https://kiricab.github.io/mdkanban/` のURLエントリを追加（`lastmod`=実装日、`changefreq`=`monthly`、`priority`=`0.80`）
  5. ルート `index.html` の `lastmod` を実装日に更新
  6. 4種JSON-LD（WebApplication / BreadcrumbList / FAQPage / HowTo）を必ず設置
  7. `seo-content` セクションに「使い方」「よくある質問」を必ず含める
  8. preconnect: GTM・AdSense・採用CDNドメイン
  9. GA（`G-CN3KBWSXRE`）・AdSense（`ca-pub-8141179596557780`）スニペットを `<head>` に埋め込む
- **確認が必要な未確定事項**:
  - ルート `index.html` のtools-gridカードに使うアイコン絵文字（候補：📋 / 🗂️ / 🪧）
  - ルートカードの説明文（30〜50字程度のキャッチー版）— 実装時にplannerと相談可
